//! OpenSkyrim Converter Library
//! Handles offline asset transformation: .nif -> .glb, .dds -> KTX2, .esm -> libSQL, .pex -> Luau.

pub struct AssetPipeline;

impl AssetPipeline {
    pub fn run() {
        println!("OpenSkyrim Converter Pipeline initialized.");
    }
}
